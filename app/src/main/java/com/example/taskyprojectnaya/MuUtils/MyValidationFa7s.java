package com.example.taskyprojectnaya.MuUtils;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

/**
 * מחלקה שבודקת תקינות שדות כמו אימיל, סיסמא ועוד
 */
public class MyValidationFa7s
{
  private Pattern pattern;
  private Matcher matcher;
  private
}
